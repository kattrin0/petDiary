<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/greenPrimary</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_paw0</item>
        <!-- После экрана загрузки переключаемся на основную тему приложения
             (она наследуется от AppCompat через Material-компоненты) -->
        <item name="postSplashScreenTheme">@style/Theme.petDiary</item>
    </style>
    <!-- //////////////////////////////////////////////////////////////////////////////////-->
    <!-- Стиль для TextInputLayout  -->
    <style name="Theme.TextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxBackgroundColor">@android:color/white</item>
        <item name="boxStrokeColor">@color/orangeLight</item>
        <item name="boxCornerRadiusTopStart">12dp</item>
        <item name="boxCornerRadiusTopEnd">12dp</item>
        <item name="boxCornerRadiusBottomStart">12dp</item>
        <item name="boxCornerRadiusBottomEnd">12dp</item>

        <item name="boxStrokeWidth">1dp</item>
        <item name="boxStrokeWidthFocused">2dp</item>
    </style>

    <!-- Стиль выпадающего списка -->
    <style name="Theme.ListView" parent="Widget.AppCompat.ListView.DropDown">
        <item name="android:scrollbars">vertical</item>
        <item name="android:listSelector">@color/orangeLight</item>
    </style>

    <!-- Стиль DatePickerDialog -->
    <style name="Theme.DatePickerDialog" parent="ThemeOverlay.MaterialComponents.Dialog">
        <item name="colorPrimary">@color/orangeLight</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSurface">@android:color/white</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
    <style name="OrangeDatePicker" parent="android:Theme.Material.Light.Dialog">
        <item name="colorAccent">@color/orangeLight</item>
    </style>
    <!-- //////////////////////////////////////////////////////////////////////////////////-->
    <style name="Base.Theme.petDiary" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>


    <style name="Theme.petDiary" parent="Base.Theme.petDiary" />
</resources>